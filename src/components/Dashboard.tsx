import { Stack, Typography } from "@mui/material";

export const Dashboard = () => {
  return (
    <Stack direction="column" gap={2}>
      <Typography variant="h1" color='info.main'> Welcome to Collaborize</Typography>
    </Stack>
  );
};
